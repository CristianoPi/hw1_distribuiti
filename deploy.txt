creare l'immagine --> docker build -t peppe2000/my_server_image:latest ./server
creare l'immagine --> docker build -t cr1000/my_server_image:latest ./server
push in docker hub --> docker push peppe2000/my_server_image:latest
push in docker hub --> docker push cr1000/my_server_image:latest

kubectl apply -f server-deployment.yaml
kubectl apply -f server-service.yaml   

in ordine:
apply db

build server
push server

apply zookeeper
apply kafka

build datacollector
push data
apply datacollector

alertsystem

___con kind____a

kind create cluster --config kind-config.yaml

load di tutte le immagini:
kind load docker-image my_datacollector_image:latest my_server_image:latest my_alertsystem_image:latest my_alertnotificationsystem_image:latest  --name my-kind-cluster


apply in ordine:("kubectl apply -f ."  in ogni cartella)
-db
-server
-kafka-all-in-one
-datacollector
-alertsystem
-alertnotificationsystem



----
query

response_time_seconds{service="datacollector", node="worker"}


total_requests_total{service="datacollector", node="worker"} numero totale di richieste nel tempo,


total_errors_total{service="datacollector", node="worker"} non mi restituisce nessun dato, forse perchp non ci sono stati errori ?


total_requests_created{service="datacollector", node="worker"} tempo passato in secondi dal tempo unix

da fare mettere metriche in alertsystem e alertnotificationsystem mappare su prometheus le rispettive porte e controlalre se le prende con le query

query per alertnotificationsystem:

Numero totale di email inviate:
emails_sent_total{service="alertnotificationsystem", node="worker"}

Numero totale di errori:
total_errors_total{service="alertnotificationsystem", node="worker"}

per provare queste due devo rifare le immagini del alartnotififcation
Numero totale di messaggi ricevuti:
messages_received_total{service="alertnotificationsystem", node="worker"}

Tempo di elaborazione dei messaggi:
message_processing_time_seconds{service="alertnotificationsystem", node="worker"}